% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_graph_sankey_tf_custom}
\alias{plot_graph_sankey_tf_custom}
\title{Plot an intracellular Receptor -> TF -> Ligand sankey from pre-scored lists}
\usage{
plot_graph_sankey_tf_custom(
  gene_list1,
  gene_list2,
  cluster,
  pagerank_table,
  score_name = "LRScore",
  min_max_val = 1,
  legend_name = "Intracellular Scoring",
  title = "Intra",
  size_node = "fixed_size",
  colors = c("#2c7bb6", "#84b0d1", "#ebebeb", "#f88e5f", "#bb001c")
)
}
\arguments{
\item{gene_list1}{data.frame of receptor -> TF edges. Must contain \code{gene_A}
(receptor), \code{gene_B} (TF), the column named by \code{score_name}, and \code{AvgTFScore}.}

\item{gene_list2}{data.frame of TF -> ligand edges. Must contain \code{gene_A}
(TF), \code{gene_B} (ligand), the column named by \code{score_name}, and \code{AvgTFScore}.}

\item{cluster}{cluster name; used to build the \code{cluster/gene} keys that are
looked up in \code{pagerank_table}.}

\item{pagerank_table}{data.frame with columns \code{nodes} (formatted as
\code{cluster/gene}) and \code{Pagerank}, indexable by rowname.}

\item{score_name}{column in the gene lists used to colour the edges.
Default "LRScore".}

\item{min_max_val}{symmetric limit for the diverging edge colour scale; edges
are clipped to \verb{[-min_max_val, min_max_val]} so colours are comparable
across plots. Default 1.}

\item{legend_name}{title of the edge-colour legend. Default "Intracellular Scoring".}

\item{title}{plot title. Default "Intra".}

\item{size_node}{node-sizing mode: \code{"fixed_size"} (all non-TF nodes equal),
\code{"score_size"} (TF nodes scaled by \code{abs(AvgTFScore)}, others fixed) or any
other value (raw pagerank). Default "fixed_size".}

\item{colors}{five-stop colour vector for the edge gradient (low -> mid -> high).}
}
\value{
A \code{ggraph}/\code{ggplot} object. Print it to display, or pass it to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} to save with any device.
}
\description{
Alternative to \code{\link[=plot_graph_sankey_tf]{plot_graph_sankey_tf()}} for the case where the
receptor -> transcription-factor and transcription-factor -> ligand edges
have already been individually filtered, ranked and scored. It takes the two
edge tables exactly as supplied and only lays out and renders the graph.
}
\examples{
\dontrun{
# gene_list1 / gene_list2 are built by the caller with any custom
# filtering and scoring, then handed to the renderer:
p <- plot_intracellular_sankey(
  gene_list1 = my_receptor_tf_edges,
  gene_list2 = my_tf_ligand_edges,
  cluster = "Tumor",
  pagerank_table = pagerank_tbl,
  score_name = "LRScore",
  size_node = "score_size"
)
p                                   # display in the current device
ggplot2::ggsave("intra.pdf", p, width = 8, height = 8,
                device = grDevices::cairo_pdf)   # or save it yourself
}
}
